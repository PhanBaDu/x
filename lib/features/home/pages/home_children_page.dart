import 'package:flutter/cupertino.dart';

class HomeChildrenPage extends StatelessWidget {
  const HomeChildrenPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Home Children Page'));
  }
}
